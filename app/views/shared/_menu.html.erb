<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to t('nav.new_post'), new_post_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('nav.view_all_posts'), posts_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= button_to t('nav.log_out'), destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger' %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to t('nav.login'), new_user_session_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('nav.sign_up'), new_user_registration_path, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>

    <!-- Language Switcher -->
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <%= t('nav.language') %>
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
          <li><%= link_to 'Español', url_for(locale: 'es'), class: 'dropdown-item' %></li>
          <li><%= link_to 'English', url_for(locale: 'en'), class: 'dropdown-item' %></li>
        </ul>
      </li>
    </ul>
  </div>
</nav>
